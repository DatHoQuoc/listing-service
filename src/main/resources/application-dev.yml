spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.yourcompany.listing: TRACE
    org.springframework.web: TRACE
    org.springframework.web.cors: TRACE
    org.springframework.web.servlet.handler: TRACE